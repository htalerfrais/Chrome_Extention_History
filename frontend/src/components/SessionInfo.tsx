// SessionInfo component for displaying session metadata
// This will show duration, time, and topic count for current session

export default function SessionInfo() {
  return (
    <div>
      {/* Session info content will go here */}
    </div>
  );
}
