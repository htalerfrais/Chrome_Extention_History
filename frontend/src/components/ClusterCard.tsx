// ClusterCard component for displaying individual clusters
// This will show a cluster theme and its items

export default function ClusterCard() {
  return (
    <div>
      {/* Cluster card content will go here */}
    </div>
  );
}
